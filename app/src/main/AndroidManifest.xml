<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="c.bilgin.anipal">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewModel.CropActivity"></activity>
        <activity android:name=".ViewModel.Account.AnipalBuyCoinActivity" />
        <activity android:name=".ViewModel.Post.AnipalPostUploadActivity" />
        <activity
            android:name=".ViewModel.NavigationActivity"
            android:label="@string/title_activity_navigation"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ViewModel.Account.RegisterActivity" />
        <activity android:name=".ViewModel.Account.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>